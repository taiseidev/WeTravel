name: we_travel
description: It is an application that fully supports your travel. Currently under development.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.1 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  salomon_bottom_bar: ^3.3.1

# UI
salomon_bottom_bar: ^3.3.1

# Navigator
go_router: ^5.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic_mono: any
flutter:
  uses-material-design: true
